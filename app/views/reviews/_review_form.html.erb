<%= form_for @review do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br />	
	<%= f.label :body %>
	<%= f.text_area :body %>
	<br />
	<%= f.label :rating %>
	<%= f.number_field :rating %>
	<br />
	<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
